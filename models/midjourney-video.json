{
  "code": 200,
  "message": "success",
  "data": {
    "components": {
      "schemas": {
        "Input": {
          "properties": {
            "aspect_ratio": {
              "default": "1:1",
              "description": "The aspect ratio of the generated media.",
              "enum": [
                "1:1",
                "4:3",
                "3:4",
                "2:3",
                "16:9",
                "1:2"
              ],
              "type": "string",
              "x-placeholder": "Select Aspect Ratio",
              "x-ui-component": "select"
            },
            "chaos": {
              "default": 0,
              "description": "Use the chaos parameter to add variety to your image results (0-100). Higher values produce more unusual and unexpected results.",
              "maximum": 100,
              "minimum": 0,
              "type": "integer",
              "x-ui-component": "slider"
            },
            "enable_base64_output": {
              "default": false,
              "description": "The random seed to use for the generation.",
              "disabled": true,
              "type": "boolean"
            },
            "image": {
              "description": "The image for generating the output.",
              "type": "string"
            },
            "last_image": {
              "description": "The last image for generating the output.",
              "type": "string",
              "x-accept": "image/*",
              "x-ui-component": "uploader"
            },
            "motion": {
              "default": "low",
              "description": "The motion of the generated media.",
              "enum": [
                "low",
                "high"
              ],
              "type": "string"
            },
            "prompt": {
              "description": "The text prompt describing the image you want to generate.",
              "type": "string"
            },
            "quality": {
              "default": 1,
              "description": "Use the quality parameter to control image detail and processing time.",
              "enum": [0.25, 0.5, 1, 2],
              "type": "number",
              "x-ui-component": "select"
            },
            "resolution": {
              "default": "480p",
              "description": "The resolution of the generated media.",
              "enum": [
                "480p",
                "720p"
              ],
              "type": "string",
              "x-placeholder": "Select Resolution",
              "x-ui-component": "select"
            },
            "seed": {
              "default": -1,
              "description": "Use the seed parameter for testing and experimentation. Use the same seed and prompt to get similar results.",
              "type": "integer"
            },
            "stylize": {
              "default": 0,
              "description": "Use the stylize parameter to control the artistic style in the image (0-1000).",
              "maximum": 1000,
              "minimum": 0,
              "type": "integer",
              "x-ui-component": "slider"
            },
            "weird": {
              "default": 0,
              "description": "Use the weird parameter to make your images quirky and unconventional (0-3000).",
              "maximum": 3000,
              "minimum": 0,
              "type": "integer",
              "x-ui-component": "slider"
            }
          },
          "required": [
            "image",
            "prompt"
          ],
          "type": "object",
          "x-order-properties": [
            "image",
            "prompt",
            "last_image",
            "resolution",
            "aspect_ratio",
            "motion",
            "quality",
            "stylize",
            "chaos",
            "weird",
            "seed",
            "enable_base64_output"
          ]
        },
        "PredictionResponse": {
          "properties": {
            "created_at": {
              "description": "ISO timestamp of when the request was created (e.g., \"2023-04-01T12:34:56.789Z\").",
              "format": "date-time",
              "type": "string"
            },
            "has_nsfw_contents": {
              "description": "Array of boolean values indicating NSFW detection for each output.",
              "items": {
                "type": "boolean"
              },
              "type": "array"
            },
            "id": {
              "description": "Unique identifier for the prediction, the ID of the prediction to get.",
              "type": "string"
            },
            "model": {
              "description": "Model ID used for the prediction.",
              "type": "string"
            },
            "outputs": {
              "description": "Array of URLs to the generated content (empty when status is not completed).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "status": {
              "description": "Status of the task: created, processing, completed, or failed.",
              "type": "string"
            },
            "urls": {
              "description": "Object containing related API endpoints.",
              "type": "object"
            }
          },
          "type": "object"
        }
      },
      "securitySchemes": {
        "apiKeyAuth": {
          "in": "header",
          "name": "Authorization",
          "type": "apiKey"
        }
      }
    },
    "info": {
      "description": "The WaveSpeed API.",
      "title": "WaveSpeed API",
      "version": "1.0.0"
    },
    "openapi": "3.0.0",
    "paths": {
      "/api/v3/midjourney/image-to-video": {
        "post": {
          "requestBody": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Input"
                }
              }
            },
            "required": true
          },
          "responses": {
            "200": {
              "content": {
                "application/json": {
                  "schema": {
                    "$ref": "#/components/schemas/PredictionResponse"
                  }
                }
              },
              "description": "The request status."
            }
          }
        },
        "x-api-name": "model_run"
      },
      "/api/v3/predictions/{request_id}/result": {
        "get": {
          "parameters": [
            {
              "in": "path",
              "name": "request_id",
              "required": true,
              "schema": {
                "description": "Request ID",
                "type": "string"
              }
            }
          ],
          "responses": {
            "200": {
              "content": {
                "application/json": {
                  "schema": {
                    "$ref": "#/components/schemas/PredictionResponse"
                  }
                }
              },
              "description": "Result of the request."
            }
          }
        },
        "x-api-name": "model_result"
      }
    },
    "servers": [
      {
        "url": "https://api.wavespeed.ai"
      }
    ]
  }
}